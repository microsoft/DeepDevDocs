post:
  description: Get the status of a model registration
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "./schemas/model_query_request.yml"
  response:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref:
